# qq-doc-clone is a file: dependency. It must be installed as a symlink, not a
# packed copy: a copy ships without its own node_modules, and Vite then fails
# to prebundle @tiptap/react, so the browser imports the raw CommonJS
# use-sync-external-store shim and crashes on the named export.
install-links=false
